---
title: 基数排序
date: 2025-11-23
tags:
  - 算法
  - 排序
---

**基数排序（radix sort）** 的思路和[[计数排序]]差不多，也是靠数数。但它多了个维度——利用数字的每一位来排。先按个位排，再按十位排，依此类推，位数全部处理完就排好了。

[[计数排序]]适用于数据量$n$较大但数据范围$m$较小的情况。
假设我们需要对$n=10^6$个学号进行排序，而学号是一个$8$位数字，这意味着数据范围$m=10^8$非常大，使用计数排序需要分配大量内存空间，而基数排序可以避免这种情况。

基数排序动画
![|基数排序|423](https://upload.wikimedia.org/wikipedia/commons/0/04/%E5%9F%BA%E6%95%B0%E6%8E%92%E5%BA%8F.gif)

## 算法流程

以学号为例，假设数字的最低位是第一位，最高位是第八位。
1. 初始化位数$k=1$。
2. 对学号的第$k$位执行“计数排序”。完成后，数据会根据第$k$位从小到大排序。
3. 将$k$增加$1$，然后返回步骤$2$，继续迭代，直到所有位都排序完成后结束。
![|基数排序算法流程|628x354](https://r2.salix.eu.org/obsidian/20260424170304372.webp)
<center><small>基数排序算法流程</small></center>
下面需要剖析一下代码实现。对于一个$d$进制的数字$x$，要获取其第$k$位$x_k$，可以使用以下计算公式：
$$
x_k = \lfloor\frac{x}{d^{k-1}}\rfloor\mod d
$$
其中$\lfloor a \rfloor$表示对浮点数$a$向下取整，而$mod\,d$表示对$d$取模（取余）。
对于用例数据，$d=10$且$k\in[1,8]$。

需要小幅改动计数排序代码，使之可以根据数字的第$k$位进行排序：


## 算法特性

基数排序适用于数值范围较大的情况，**但前提是数据必须可以表示为固定位数的格式，且位数不能过大**。

* **时间复杂度为$O(nk)$、非自适应排序**：设数据量为$n$、数据为$d$进制、最大位数为$k$，则对某一位执行计数排序使用$O(n+d)$时间，排序所有$k$位使用$O(n+d)$时间。通常情况下，$d$和$k$都相对较小，时间复杂度趋向$O(n)$。
* **空间复杂度为$O(n+d)$、非原地排序**：与[[计数排序]]相同，基数排序需要借助长度为$n$和$d$的数组$res$和$counter$。
* **稳定排序**：当计数排序稳定时，基数排序也稳定；当计数排序不稳定时，基数排序无法保证得到正确的排序结果。