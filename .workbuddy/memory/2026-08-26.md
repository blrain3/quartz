# 2026-08-26 维护方案实施完成

## 完成内容（三阶段全部完成，commit 2h5520a / bdb9761 / e8cd66d）

### 阶段一（高优先级）
- `npm audit fix` 修复安全漏洞
- `patch-graph-output.mjs` 集成到 `postbuild` 脚本
- Dockerfile builder 阶段添加 `npm run patch` 步骤
- TS 7.0.2 兼容性修复：`moduleResolution` 从已移除的 `"node"` 改为 `"bundler"`，在 `index.d.ts` 添加 `micromorph` 和 `remark-parse/lib` 模块声明（上游包 exports 字段未声明 types 条件）

### 阶段二（补丁脚本加固）
- patch-graph.mjs / patch-search.mjs / patch-footer.mjs / patch-graph-output.mjs 全部添加 `quartz.lock.json` SHA 版本检测 + 模式不匹配时 exit 1 失败断言 + 正则回退
- 已知插件 commit：graph `46f0ba1`、footer `6ed6192`、search `0f4c1a2`

### 阶段三（中低优先级）
- MyFooter.tsx 重写直接返回 `blrain 2026`，patch-footer.mjs 从 prebuild/patch 移除（footer 单一来源）
- GraphButton.tsx 添加 UNUSED 状态注释
- 新增 `.dockerignore`（排除 public/、node_modules/、.quartz/、docs/ 等）
- custom.scss 添加字体平滑 + 中文排版（`overflow-wrap: break-word; line-height: 1.8`，弃用方案中的 `word-break: break-all` 因过于激进）
- ignorePatterns 添加 `.claudian`

## 依赖检查发现（重要）
- `@quartz-community/types` 上游 `0f5711d`（2026-07-27）含**破坏性变更**：`PageFrameProps.footer` 改为 `QuartzComponent[]`，暂不更新
- `@quartz-community/utils` 上游 `9da5e8d` 含 `getFullSlugFromUrl` 非 ASCII slug 的 URI 解码修复 — 但补丁针对的是 graph 插件 dist 内已编译内联代码，更新 npm utils 无法惠及；待 graph 插件发新版后可移除 patch-graph*.mjs

## 环境注意事项
- Windows 环境，项目在 `D:\Project\Work\quartz`
- 构建验证通过：137 文件、footer 渲染 `<footer><p>blrain 2026</p></footer>`、.claudian 未进入产物、自定义样式已编译
- `patch-graph-output` 在 graph 禁用时输出 "Pattern not found" 属预期非致命行为
